{
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.3",
    "@vercel/node": "^5.3.24"
  },

"scripts": {
  "vercel-build": "cd frontend && npm ci && npm run build && if [ -d build ]; then cp -a build ../public; elif [ -d dist ]; then cp -a dist ../public; fi && cd ../backend && npm ci && npm run build"
}


}
