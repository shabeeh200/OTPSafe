{
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/dotenv": "^6.1.1",
    "@types/express": "^5.0.3",
    "@vercel/node": "^5.3.24"
  },

  "scripts": {
  "vercel-build": "cd frontend && npm ci && npm run build && cp -a dist ../public && cd ../backend && npm ci && npm run build"

  }


}
